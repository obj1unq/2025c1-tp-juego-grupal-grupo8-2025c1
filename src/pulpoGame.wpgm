import wollok.game.*
import estadoDeJuego.*
import entrada.*

program pulpoGame {
	game.title("Pulpo Game")
	game.height(8)
	game.width(13)
	game.cellSize(120)
	game.boardGround("fondo.png")

	entrada.inicializar()
	estadoDeJuego.inicializar()

	game.start()
}